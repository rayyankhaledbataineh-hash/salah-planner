{
  "name": "salah-planner",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cloudFunction.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "deploy": "npm run build && gcloud functions deploy salah-planner --gen2 --region=us-central1 --runtime=nodejs20 --source=. --entry-point=updatePrayerTimes --trigger-http --no-allow-unauthenticated --memory=256Mi --timeout=60s",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.5.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@googleapis/calendar": "^15.0.0",
    "axios": "^1.16.1",
    "dotenv": "^17.4.2",
    "google-auth-library": "^10.6.2",
    "googleapis": "^173.0.0",
    "open": "^11.0.0"
  }
}
